Problema 1067 URI

int x = int.Parse(Console.ReadLine());
if (x < 1000)
{
    for (int i = 0; i < x; i++)
    {
        if (i % 2 != 0)

            Console.WriteLine(i);
        
    }
    if (x % 2 != 0)
        Console.WriteLine(x);
}
